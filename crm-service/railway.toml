[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build && node -e \"require('./dist/db/seed.js')\" 2>/dev/null || true && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
